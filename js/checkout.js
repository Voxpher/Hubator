async function startCheckout(form) { const lines = cartLines(); if (!lines.length) return showToast("Your cart is empty"); const value = (name) => form.elements[name].value.trim(); const payload = { customer: { name: `${value("firstName")} ${value("lastName")}`.trim(), email: value("email") }, shippingAddress: { name: `${value("firstName")} ${value("lastName")}`.trim(), line1: value("address"), city: value("city"), state: value("state") || value("city"), postalCode: value("postalCode"), country: value("country") }, items: lines.map((line) => ({ productId: line.product.id, quantity: line.qty })), shippingMethod: "Standard", paymentMethod: "cod" }; const response = await fetch(hubatorApiUrl("/api/public/orders"), { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(payload) }); const data = await response.json().catch(() => ({})); if (!response.ok) throw new Error(data.error || "Unable to place the order."); localStorage.removeItem(CART_KEY); showToast(`Order ${data.orderNumber} created successfully.`); setTimeout(() => { window.location.href = "index.html"; }, 1500); }
